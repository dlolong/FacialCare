# Keep secrets out of git. Add real values only in .env.local.
# Set this to the final HTTPS domain to enable search-engine indexing and canonical URLs.
# Localhost and missing values keep the owner-review website set to noindex.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
